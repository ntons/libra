# LDS for dynamic listener configuration

resources:
- name: listener_apiproxy
  "@type": type.googleapis.com/envoy.config.listener.v3.Listener
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 80
  filter_chains:
  - filters:
    - name: envoy.filters.network.http_connection_manager
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
        stat_prefix: listener_apiproxy
        route_config:
          name: route_apiproxy
          virtual_hosts:
          - name: route_apiproxy
            domains:
            - "*"
            routes:
            - match:
                safe_regex:
                  google_re2: {}
                  regex: '/libra\.v1\.(BubbleChart|Database|Distlock|Gateway|Leaderboard|Mailbox)/.*'
                grpc: {}
              route:
                cluster: cluster_librad_v1
                idle_timeout: 5s # must be disabled for long-lived and streaming requests
        http_filters:
        - name: envoy.filters.http.ext_authz
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.http.ext_authz.v3.ExtAuthz
            grpc_service:
              envoy_grpc:
                cluster_name: cluster_librad_v1
              timeout: 0.5s
            transport_api_version: V3
        - name: envoy.filters.http.router


