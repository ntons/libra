###############################################################################
# Destination rule
###############################################################################
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: portal-v1
  namespace: libra
spec:
  host: portal-v1
  subsets:
  - name: v0-0-1
    labels:
      version: v0.0.1
---

###############################################################################
# Service
###############################################################################
apiVersion: v1
kind: Service
metadata:
  name: portal-v1
  namespace: libra
  labels:
    app: portal-v1
spec:
  ports:
  - port: 80
    name: grpc
  selector:
    app: portal-v1
---

###############################################################################
# Version tagged workloads
###############################################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal-v0-0-1
  namespace: libra
  labels:
    app: portal-v1
    version: v0.0.1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: portal-v1
      version: v0.0.1
  template:
    metadata:
      labels:
        app: portal-v1
        version: v0.0.1
    spec:
      containers:
      - name: librad
        image: ntons/librad:0.0.1
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: librad-env
          prefix: LIBRAD_
        command: ['./librad', '-c', './librad.yaml', '-i', 'portal']
        #command: ['sleep', '3600']
        ports:
        - containerPort: 80
---

